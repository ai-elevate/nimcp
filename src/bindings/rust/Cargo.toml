[package]
name = "nimcp"
version = "2.6.3"
edition = "2021"
authors = ["NIMCP Contributors"]
description = "Rust bindings for NIMCP - Neural Interface Message Communication Protocol"
license = "MIT"

[dependencies]

[build-dependencies]

[lib]
name = "nimcp"
path = "src/lib.rs"
