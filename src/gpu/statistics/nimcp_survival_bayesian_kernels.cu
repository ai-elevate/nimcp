/**
 * @file nimcp_survival_bayesian_kernels.cu
 * @brief CUDA kernels for survival and Bayesian analysis (stub)
 *
 * NOTE: This is a stub file. Full implementation pending.
 */

#ifdef NIMCP_ENABLE_CUDA

#include <cuda_runtime.h>
#include "gpu/common/nimcp_cuda_utils.h"
#include "gpu/recovery/nimcp_gpu_recovery.h"
#include "utils/exception/nimcp_exception_macros.h"

/* Stub file - implementation pending */
/* When implementing, use:
 *   - NIMCP_CUDA_RECOVER(cudaMalloc(...), GPU_ERROR_OUT_OF_MEMORY) for memory allocation
 *   - NIMCP_CUDA_RECOVER(cudaMemcpy(...), GPU_ERROR_CUDA_RUNTIME) for memory copies
 *   - NIMCP_CUDA_RECOVER_LAST(GPU_ERROR_KERNEL_LAUNCH) after kernel launches
 *   - nimcp_gpu_recovery_is_initialized() / nimcp_gpu_recovery_init(NULL) at function entry
 */

#endif
